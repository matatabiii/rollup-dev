<%_
const root = './'
include(root + '_data/_require.ejs')

/* ページ情報 */
const data   = init({
  root       : root, // ルートディレクトリへの相対パス
  title      : '内部ページタイトル', // ページタイトル
  description: 'ディスクリプションが入ります', // ディスクリプション
  slug       : '', // ページ情報判断するためのslug（default: home）
  pageType   : '', // ページタイプを判断（ default: page | single ）
  file       : '', // カノニカルurl等に使用、htmlとしてレンダリングされた場合のルートからの実際ファイル名（index等は省略）
  titleTag   : '', // titleを設定すると勝手にタイトルタグを作りますが、それを上書きする場合はこちらに
  ogType     : 'website', // og:type（default:article）
  bodyClass  : '', // body追加したいclass
  hero       : { // ヒーロー用
    parent   : '', // 親ページタイトル等
    // main  : '', // メインタイトル 省略することで titile を継承する
    sub      : '', // 英字表記・サブタイトル等
    bgImage  : '' // 背景イメージファイル名（画像は assets/images/common/ に設置想定）
  }
})

/* 生データ上書き */
// data.head.canonical = false
// data.head.og.url = false

/* 扱いやすくデータを変数に */
const fvGet = data.get(data)
_%>

<%_　// メインコンテンツ
fvGet.components.main = function (__append) { _%>
  <h2>メインコンテンツ</h2>

  <div class="c-of-hidden c-mb30">
    <div class="c-container-just">
      <ul class="c-flex c-flex--x1">
        <li>
          <a
            href="<%= fvGet.root %>sample/"
            class="p-btn p-btn--radius p-btn--w130 p-btn--h36 p-btn--text"
          >
            <i class="fas fa-hospital-alt p-btn__i c-mr5"></i>
            <span class="p-btn__label u-700">サンプル</span>
          </a>
        </li>

        <li>
          <a
            href="<%= fvGet.root %>sample/"
            class="p-btn p-btn--radius p-btn--w130 p-btn--h36 p-btn--text"
          >
            <i class="fas fa-user-friends p-btn__i c-mr5"></i>
            <span class="p-btn__label u-700">サンプル</span>
          </a>
        </li>

        <li>
          <a
            href="<%= fvGet.root %>sample/"
            class="p-btn p-btn--radius p-btn--w130 p-btn--h36"
          >
            <span class="p-btn__label u-700">サンプル</span>
          </a>
        </li>

        <li>
          <a
            href="<%= fvGet.root %>sample/"
            class="p-btn p-btn--radius p-btn--w130 p-btn--h36 p-btn--danger p-btn--o"
          >
            <span class="p-btn__label u-700">サンプル</span>
          </a>
        </li>
      </ul>
    </div>
  </div>

  <h2>【TODO - 2020.09.11時点】</h2>
  <ul>
    <li>カレントナビステータス管理（JS）</li>
    <li>ドロワーロジック（JS・CSS）</li>
    <li>カルーセルスライダーロジック（JS・CSS）</li>
    <li>フェードスライダーロジック（JS）</li>
    <li>ギャラリースライドロジック（横にループするやつ）（JS・CSS）</li>
    <li>アコーディオンーロジック（JS・CSS）</li>
    <li>タブロジック（JS・CSS）</li>
    <li>スムーススクロール（JS）</li>
  </ul>
<%_ } _%>

<%_
  // fvGet.sideType = 'default' // サイドコンテンツを出力する場合はこれをコメントアウト
  fvGet.components.side = function (__append) {
_%>
  <%- // サイドコンテンツ
    include('./../_componets/_sidebar.ejs', { fvGet })
  %>
<%_ } _%>

<%- include(root + '_layout/_default.ejs', {　fvGet　}) %>
